# Pipeline Artifact Sessions

This directory contains structured outputs from each pipeline run.

Each session folder is named `<YYYY-MM-DD-task-slug>/` and contains artifacts from every stage of the pipeline.

See `../learnings/` for accumulated cross-session insights.
